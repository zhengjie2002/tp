package seedu.sgsafe.utils.command;

public enum SettingType {
    // Date input is the format for parsing dates from user input
    DATEINPUT,

    // Date output is the format for displaying dates to the user
    DATEOUTPUT,

    // Timestamp output is the format for displaying date and time to the user for audit logs
    TIMESTAMPOUTPUT
}
